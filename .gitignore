__pycache__/
*.pyc
*.pyo
.pytest_cache/
*.egg-info/
.venv/
venv/
.DS_Store
PROGRESS_LOG.md
configs/*.yaml
!configs/config.example.yaml

# per-script local path configs (see mask_tools/paint_mask_local.example.yaml) --
# real paths live in *_local.yaml, gitignored so editing them isn't a git diff.
*_local.yaml

# scripts/_form_dialog.py's remembered last-used form values, per script
scripts/.dialog_state/
