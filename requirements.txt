antspyx
brainglobe-atlasapi
tifffile
zarr
dask
SimpleITK
scikit-image
nibabel
pystripe
pandas
pyyaml

# The interactive annotation/QC tools now live in ../GT_tool_for_registration,
# but they still run in this same antsreg env: they import registration_ants
# (via its editable install) and registration_eval.py needs antspyx. Keep napari
# here so rebuilding antsreg from this file leaves those tools runnable.
napari[pyqt5]
